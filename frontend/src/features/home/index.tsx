import { Box } from '@mui/material';

const Home = () => {
  return (
    <Box
      display="flex"
      sx={{
        flexDirection: 'column',
        width: '100%',
        height: '100%',
        padding: 2,
      }}
      gap={1}
    >
      Home
    </Box>
  );
};

export default Home;
